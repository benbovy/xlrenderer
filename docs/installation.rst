============
Installation
============

At the command line::

    $ easy_install xlrenderer

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv xlrenderer
    $ pip install xlrenderer
